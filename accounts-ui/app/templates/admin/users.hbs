<div class="container"> 
  <div class="page-header">
    {{#if isList}}
      <h1>{{t 'user.list.header'}}</h1>
      <hr>
      <div class="row">
        <div class="col-xs-4">
          {{t 'user.user-account-filter'}}
        </div>
        <div class="col-xs-5"> 
          {{#power-select 
              options=statusFilterOptions 
              placeholder="Select a status filter"
              selected=selectedFilter
              searchEnabled=false
              allowClear=true
              onchange=(action "statusChange"  ) as |filter| }}
            {{filter.status}}
          {{/power-select}} 
        </div>
        <div class="col-xs-3">
        </div> 
      </div>  

      <br >
      <div class="row">
        <div class="col-xs-4">
          Find all logged in user: 
        </div>
        <div class="col-xs-5"> 
          {{input type="checkbox" checked=isLoggedIn click=(action "loggedInUser")}}
        </div>
      </div>
    {{/if}}

  </div>
 
  {{outlet}}
</div>
